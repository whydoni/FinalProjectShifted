<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginActivity"
        android:usesCleartextTraffic="true">
        <activity android:name="com.example.loginactivity.MutasiActivity"></activity>
        <activity android:name="com.example.loginactivity.CreditActivity" />
        <activity android:name="com.example.loginactivity.RegisterActivity" />
        <activity android:name="com.example.loginactivity.HomeActivity" />
        <activity android:name="com.example.loginactivity.LoginActivity" />
        <activity android:name="com.example.loginactivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>